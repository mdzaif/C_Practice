#include<stdio.h>

/*int main()
{
    int n, k, i, count = 0;
    scanf("%d", &n);
    int a[n];
    scanf("%d", &k);

    for(i = 0; i<n; i++)
    {
        scanf("%d", &a[i]);
    }
    i = 0;
    while(i < n)
    {
        if(a[i]>= a[k-1] && a[i] > 0)
        {
            count++;
        }
        i++;
    }
    printf("%d\n", count);

    return 0;
}*/
int main()
{
    printf("Actual Problems in codeblocks\n");
}
